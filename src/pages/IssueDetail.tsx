import { Link, useParams } from "react-router-dom";
import { useWorkspace } from "../WorkspaceContext";
export default function IssueDetail(){const {id}=useParams();const {workspace}=useWorkspace();const issue=workspace.issues.find(item=>item.id===id);if(!issue)return <div className="page empty"><h1>Operating record not found.</h1><Link to="/issues">Return to issues</Link></div>;return <div className="page detail"><div className="detail-top"><Link to="/issues">← Issue register</Link><span>{issue.id} · {issue.status}</span></div><header><p className="eyebrow">{issue.area} · {issue.shift} shift</p><h1>{issue.title}</h1><div className="detail-tags"><b data-severity={issue.severity}>{issue.severity}</b><span>{issue.equipment}</span><span>Owner · {issue.owner}</span><span>Due · {issue.due.replace("T"," ")}</span></div></header><div className="detail-grid"><section><p className="eyebrow">Observation</p><h2>What changed</h2><p>{issue.observation}</p></section><section className="decision"><p className="eyebrow">Carryover decision</p><h2>What the next shift must protect</h2><p>{issue.decision}</p></section><section><p className="eyebrow">Evidence</p><h2>What supports the record</h2><p>{issue.evidence||"No resolution evidence has been recorded yet."}</p></section></div><section className="audit"><p className="eyebrow">Immutable audit history</p><h2>How the record changed</h2>{issue.audit.slice().reverse().map(entry=><article key={entry.id}><time>{entry.at.replace("T"," ")}</time><div><strong>{entry.event}</strong><span>{entry.actor}</span>{entry.evidence&&<p>{entry.evidence}</p>}</div></article>)}</section></div>}
